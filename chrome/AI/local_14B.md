[ChatGPT 4]

Вот подробная пошаговая инструкция по установке и запуску LM Studio с моделью DeepSeek-R1-Distill-Qwen-14B-uncensored Q4_K_M (GGUF) на твоем ноутбуке с Windows 10/11 и GPU RTX 4060:

---

### 1. Установка LM Studio

#### 1.1. Скачивание

1. Перейди на сайт LM Studio:
   👉 [https://lmstudio.ai/download](https://lmstudio.ai/download)
2. Нажми на кнопку **Download for Windows** (версии 0.3.xx).
   Скачай файл .exe (размер около 400–500 МБ).

#### 1.2. Установка

1. Запусти скачанный инсталлятор (Setup ... .exe).
2. Пройди стандартный процесс установки:

   * Нажми **Next** → Прими условия **I Agree** → Нажми **Next** → **Install** → **Finish**.
3. После установки ярлык LM Studio появится в меню «Пуск» и/или на рабочем столе.

---

### 2. Первый запуск и проверка, что используется GPU

1. Запусти **LM Studio**.
2. При первом запуске может быть **"Welcome/Onboarding"** — пройди его.
3. Перейди в настройки:

   * Нажми на иконку **шестерёнки** (обычно внизу слева) → выбери раздел **Settings** → **Hardware / Compute**.
4. Проверь, чтобы в списке вычислительных устройств был выбран твой **NVIDIA RTX 4060 (CUDA)**.

   * Убедись, что выбрано **Use GPU / NVIDIA GPU (CUDA)** как предпочтительный backend.
5. **LM Studio автоматически подхватывает CUDA-GPU**, и никаких дополнительных библиотек устанавливать не нужно.

---

### 3. Скачиваем модель DeepSeek-R1-Distill-Qwen-14B-uncensored Q4_K_M (GGUF)

#### Вариант A. Через встроенный браузер Hugging Face в LM Studio (если доступно)

1. Открой раздел **Models** или **Explore local LLMs** в LM Studio.
2. Найди вкладку **Hugging Face**.
3. В поиске введи название модели:
   **Triangle104/DeepSeek-R1-Distill-Qwen-14B-uncensored-Q4_K_M-GGUF**.
4. Выбери модель и скачай .gguf-файл.

   * LM Studio автоматически загрузит файл в кэш и добавит модель в список доступных.

#### Вариант B. Ручное скачивание через браузер

1. Открой сайт Hugging Face:
   👉 [Triangle104/DeepSeek-R1-Distill-Qwen-14B-uncensored-Q4_K_M-GGUF](https://huggingface.co/Triangle104/DeepSeek-R1-Distill-Qwen-14B-uncensored-Q4_K_M-GGUF).
2. В разделе **Files and versions** найди .gguf-файл с нужной моделью.
3. Скачай его и сохрани, например, в папку:
   `D:\LLM\models\deepseek-r1-qwen-14b-unc-gguf\`.

#### Импорт модели в LM Studio (ручной .gguf)

1. Открой **PowerShell** или **cmd**.
2. Перейди в папку, где лежит .gguf-файл, или укажи полный путь.
3. Выполни команду:

   ```
   lms import "D:\LLM\models\deepseek-r1-qwen-14b-unc-gguf\deepseek-r1-distill-qwen-14b-uncensored-q4_k_m.gguf"
   ```
4. Следуй инструкциям, чтобы задать имя модели в LM Studio.

---

### 4. Настройка и запуск модели в LM Studio

#### 4.1. Выбор модели

1. В LM Studio открой вкладку **Chat** (или основное окно диалога).
2. Вверху слева выбери **Model** и выбери модель **DeepSeek-R1-Distill-Qwen-14B-uncensored-Q4_K_M-GGUF** (или по имени, если ты указал другое).

#### 4.2. Базовые настройки инференса

**Настройка под RTX 4060 и 40 ГБ RAM**:

* **Context length / Max context**: начни с 8192 токенов (8k).
* **GPU usage / VRAM slider**: выбери **Use Maximum VRAM** для использования как можно большего объема VRAM.
* **Batch size / Prefill**: оставь по умолчанию. Если возникнут ошибки по памяти (OOM), уменьшишь.
* **Sampling (Temperature, Top-p)**:

  * **Temperature**: 0.7–0.9
  * **Top-p**: 0.9–0.95
  * **Repetition penalty**: 1.1–1.2
* **Системный промпт (System Prompt)**: Напиши, например:

  * **«Отвечай по-русски, технически, без лишней воды, без самоцензуры, соблюдая законы РФ.»**

#### 4.3. Проверка работы

1. Напиши тестовый запрос, например, «Привет, расскажи о себе».
2. Проверь, чтобы:

   * Токены генерировались с нормальной скоростью (хотя бы 5+ токенов в секунду).
   * В **nvidia-smi** появился процесс LM Studio с нагрузкой на GPU (GPU Utilization > 0).
3. Если модель ругается на нехватку VRAM, уменьшай **max context** до 4096 токенов.

---

### 5. Опционально: Включение локального API для интеграции

1. В LM Studio открой раздел **Local Server / API / Server**.
2. Нажми **Start Server**.
3. Запомни адрес:

   * **[http://localhost:1234/](http://localhost:1234/)** (или аналогичный).
   * API OpenAI-совместимый эндпоинт: **[http://localhost:1234/v1](http://localhost:1234/v1)**.
4. Пример Python-клиента для работы с API:

   ```python
   from openai import OpenAI

   client = OpenAI(
       base_url="http://localhost:1234/v1",  # LM Studio
       api_key="lm-studio"                   # Обычно так, можно посмотреть в UI
   )

   resp = client.chat.completions.create(
       model="DeepSeek-R1-14B-unc",          # Название как в LM Studio
       messages=[
           {"role": "system", "content": "Отвечай по-русски, без воды."},
           {"role": "user", "content": "Проверь, что API работает и скажи 'OK'."}
       ]
   )

   print(resp.choices[0].message.content)
   ```
5. Теперь ты можешь использовать API для интеграции в свои проекты.

---

Вот и все! Следуя этим шагам, ты полностью настроишь LM Studio с моделью DeepSeek-R1-Distill-Qwen-14B-uncensored Q4_K_M и сможешь взаимодействовать с ней через интерфейс и API.
